import s from './style.module.css';

export default function Sponsor() {
    return(
        <section>
            <h2 className={`${s.section__title} container`} >
            Sponsorlar
            </h2>
        </section>
    )
}